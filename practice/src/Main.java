import java.util.Optional;

public class Main {

    public static void main(String[] args) {
        Weekday weekday = Weekday.valueOf(Weekday.MON.name());
        System.out.println();
        Optional<Integer> x = Optional.ofNullable(5);
        x.
        x.orElseGet(()-> 5);
    }
}